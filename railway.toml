[build]
builder = "nixpacks"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[healthcheck]
# Railway Flask portini tekshiradi
httpPath = "/health"
timeoutSeconds = 10
